# Digital Archaeologist

The `macos` directory contains the Xcode-oriented build entry point. The project deliberately checks the active Command Line Tools developer directory before invoking CMake.

See `docs/MACOS.md` for setup and troubleshooting.
